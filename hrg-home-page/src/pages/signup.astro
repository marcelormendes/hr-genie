---
const dashboardUrl =
  import.meta.env.PUBLIC_DASHBOARD_URL || "http://localhost:4322";
const registerUrl = `${dashboardUrl.replace(/\/?$/, "")}/register`; // Ensure no double slash

return Astro.redirect(registerUrl, 307); // Use 307 Temporary Redirect
---

<!-- This page intentionally left blank -->
